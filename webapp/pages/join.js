

export default function Join(){
    return(
        <div>
            {/* This button would go in the card we need to store which conrtact they select. */}
            <button><a href='/join_raffle'>Join Raffle</a></button>
            Current Raffles:
            {/* Figure out later just need cards and a for loop for each one the owner has deployed. */}
        </div>
    )
}